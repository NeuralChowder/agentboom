{
  "name": "agentboom-js",
  "private": true,
  "version": "0.7.0",
  "description": "JavaScript/TypeScript packages for agentboom (design system + dashboard). Python tooling lives in pyproject.toml; this workspace only groups the JS packages.",
  "workspaces": [
    "ui",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build -w ui && npm run build -w dashboard",
    "build:ui": "npm run build -w ui",
    "dev:dashboard": "npm run dev -w dashboard"
  }
}
