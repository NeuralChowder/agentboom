"""Connector libraries (agentboom connector packages).

A connector is an importable client for an external service. Mini-apps
import them directly (`from connectors.continente import search`). Connector
code is NOT hot-reloaded — restart the platform after changing one.
"""
