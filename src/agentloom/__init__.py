"""agentloom — scaffold, maintain, and upgrade agent projects from a shared base.

Pure Python stdlib. No runtime dependencies, so agents and CI can run it
anywhere python3 >= 3.10 exists.
"""

__version__ = "0.2.1"
