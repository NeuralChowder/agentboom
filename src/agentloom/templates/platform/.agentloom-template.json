{
  "template": "platform",
  "description": "Full agent stack: Qwen Code agent home (.qwen-docker/) + Python mini-app platform (gateway, SDK, scheduler) + Docker deployment.",
  "managed": [
    "platform/sdk/*",
    "platform/services/scheduler.py",
    "platform/migrations/run.py",
    "platform/scripts/*",
    "platform/README.md",
    ".qwen-docker/output-language.md",
    ".qwen-docker/agents/*"
  ],
  "executable": [
    "entrypoint.sh",
    "platform/scripts/prune_agent_transcripts.py"
  ],
  "notes": {
    "managed": "Patterns are fnmatch-style against agent-relative paths ('*' also matches '/'). Base skills copied into .qwen-docker/skills/ are always managed regardless of these patterns.",
    "user_owned": "Everything else (AGENTS.md, compose, Dockerfiles, miniapps, migrations 002_+) belongs to the agent from init onwards."
  }
}
