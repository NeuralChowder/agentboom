{
  "name": "@agentboom/sdk",
  "version": "0.8.0",
  "description": "agentboom SDK for Node/TypeScript mini-apps. This is a BRIDGE, not a re-implementation: all shared logic (db, llm, agent, capabilities) lives once in the platform gateway, and this package is a thin loopback-HTTP client onto it. No duplicated logic, no native dependencies.",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test 'tests/**/*.mjs'"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NeuralChowder/agentboom",
    "directory": "sdk-ts"
  }
}
